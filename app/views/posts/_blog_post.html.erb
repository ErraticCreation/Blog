<% @posts.each do |post| %>
  <div class="anchor" id="post<%= post.id %>"></div>
  <section class="panel panel-default">
    <header class="panel-heading">
      <h3><%= post.title %></h3>
    </header>
    <article class="panel-body">
      <p><%= post.body %></p>
    </article>
    <footer>
      <!-- Future Comments Preview -->
    </footer>
  </section>
<% end %>